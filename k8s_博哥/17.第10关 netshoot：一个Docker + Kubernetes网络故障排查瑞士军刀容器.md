https://github.com/nicolaka/netshoot

# k8s 如何使用

```
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: netshoot
  name: netshoot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netshoot
  template:
    metadata:
      labels:
        app: netshoot
    spec:
      containers:
      - image: docker.io/nicolaka/netshoot
        name: netshoot
        args:
        - /bin/bash
        - -c
        - >
           while :; do
             echo "[$(date +%F\ %T)] hello"
             sleep 1
           done


```

# 离线镜像操作

# containerd 需要指定命令空间导入镜像

# docker pull nginx:1.21.6 && docker save nginx:1.21.6 > nginx-1.21.6.tar

# ctr -n k8s.io images import nginx-1.21.6.tar

docker pull docker.io/nicolaka/netshoot

docker save nicolaka/netshoot > netshoot-latest.tar

ctr -n k8s.io images import netshoot-latest.tar

# 进到容器中去做测试

kubectl exec -it netshoot-659d7f6dcf-fq22z -- bash
tcpdump -i eth0

另外开一个 20 终端
kubectl exec -it nginx-6f648b8457-pfjjw -- bash
root@nginx-6f648b8457-pfjjw:/# curl 172.20.217.115
curl: (7) Failed to connect to 172.20.217.115 port 80: Connection refused # 这是正常的，老师也同样效果
