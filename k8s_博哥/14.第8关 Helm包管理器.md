# 使用频率

真正使用不太多，可能跟业务有关，再云原生服务安装时候用的多一些

# helm

https://helm.sh/zh/

https://github.com/helm/helm/releases

https://github.com/bitnami/charts

我们经常涉及到的资源类型的 configmaps、secrets、pv、pvc、deployment、service、ingress 这么几种组合一起使用，那么简单来说，helm 也是用来打包这些资源，提供给我们来安装。

# 几百个 yaml 就够用了，可以不用 helm，就不要用了

```
# 下载Helm  download helm
https://github.com/helm/helm/releases

# 在我们二进制安装好k8s后，其实已经有了helm这个二进制包了
# ll /etc/kubeasz/bin/helm
-rwxr-xr-x 1 root root 50597888 Aug 11 04:19 /etc/kubeasz/bin/helm*

# 做下软链接到bin目录
# ln -svf /etc/kubeasz/bin/helm /usr/bin/
'/usr/bin/helm' -> '/etc/kubeasz/bin/helm'

# 现在就可以直接helm使用了
# which helm
/usr/bin/helm

# 看下当前helm版本
# helm version
version.BuildInfo{Version:"v3.12.3", GitCommit:"3a31588ad33fe3b89af5a2a54ee1d25bfe6eaa5e", GitTreeState:"clean", GoVersion:"go1.20.7"}

# 添加helm命令补齐命令
echo 'source <(helm completion bash)' >> ~/.bashrc && . ~/.bashrc

# 添加国内适合的helm安装源
helm repo add kaiyuanshe http://mirror.kaiyuanshe.cn/kubernetes/charts
helm repo add azure http://mirror.azure.cn/kubernetes/charts
helm repo ls
# 更新下安装源
helm repo update

# 我们这里以安装mysql为例
helm search repo mysql

# 先来模拟安装一下，看看会显示什么
helm install --namespace kube-system  bogemysql ./mysql-1.6.9.tgz --dry-run --debug

install.go:200: [debug] Original chart version: ""
install.go:217: [debug] CHART PATH: /root/.cache/helm/repository/mysql-1.6.9.tgz

......





```

# 操作

```
二进制方式已经自带 helm
ll /etc/kubeasz/bin/helm

配置软连接到/usr/bin/下
ln -svf /etc/kubeasz/bin/helm /usr/bin/ # 就可以直接用 helm 命令了
helm version

# 添加 helm 命令补齐命令

echo 'source <(helm completion bash)' >> ~/.bashrc && . ~/.bashr  # 命令自动补齐仍然没好用，有待研究

# 添加国内适合的helm安装源
helm repo add kaiyuanshe http://mirror.kaiyuanshe.cn/kubernetes/charts
helm repo add azure http://mirror.azure.cn/kubernetes/charts
helm repo ls

# 更新下安装源
helm repo update

# 我们这里以安装mysql为例
helm search repo mysql

# 先来模拟安装一下，看看会显示什么
helm install --namespace kube-system  bogemysql ./mysql-1.6.9.tgz --dry-run --debug # 输出所有yaml配置

cd /root/.cache/helm/repository/
helm pull azure/mysql --version 1.6.9 # 老师并未执行这句，我先下载一下包，要不 helm install 执行有错误 ，

helm install --namespace kube-system  bogemysql azure/mysql --dry-run --debug  #可以直接指定在线的包 ，老师有改正

tar ztvf mysql-1.6.9.tgz # 没有解压，知识列出来里面有啥文件

tar xf mysql-1.6.9.tgz # 会解压

修改 values.yaml 后
helm install --namespace kube-system  bogemysql ./values.yaml  ../mysql-1.6.9.tgz --dry-run --debug

https://blog.csdn.net/weixin_46887489/article/details/134452047

```

提到了一个有状态服务 安装用 operator 的东西
