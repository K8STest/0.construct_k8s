kubectl create deployment nginx --image=docker.io/library/nginx:1.21.6

# 扩容 pod 的数量

# kubectl scale deployment nginx --replicas=2

deployment.apps/nginx scaled

curl 172.20.139.79

# 我们先来创建一个 service

# kubectl create service clusterip nginx --tcp=80:80 --dry-run=client -o yaml

# --dry-run=client -o yaml 参数可以允许临时的看一下 yaml 格式 ，但是这个操作不会真正执行哈，

kubectl create service clusterip nginx --tcp=80:80 # 真正执行

# 基于 label 去查找 pod

kubectl get po -l app=nginx

# 先修改 nginx 容器内部的 index.html 为不同的内容，然后 curl 服务 ip，会测试出来轮训的效果

curl 10.68.232.57

systemctl daemon-reload
systemctl restart kubelet

kubectl -n default set image deployment nginx nginx=docker.io/library/nginx:1.25.1 --record

kubectl get rs

# 查看历史更新版本

kubectl rollout history deployment nginx

# 回退到历史版本

kubectl rollout undo deployment nginx --to-revision=3

# curl pod 的 IP 查看版本

curl 172.20.217.92/1

kubectl rollout history deployment nginx | tail -2
