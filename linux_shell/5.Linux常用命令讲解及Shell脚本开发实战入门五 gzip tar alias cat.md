#

https://www.toutiao.com/article/7273477708280168960

大家好，我是博哥爱运维。这节课给大家带来这些 Linux 操作命令， 详细讲解请看我个人主页下同名视频教程:

压缩软件 gzip , tar

命令别名 alias

查看文件 cat

```
gzip
    gzip -kv 1.log   # k 保留源文件 ，v 显示过程
    gzip -d 1.log.gz # gzip -kd 1.log.gz k 保留压缩文件
tar
	tar ztvf
	tar zxvf  or -xf   , --exclude=

#!/bin/bash
# dd if=/dev/zero of=./1.log bs=10M count=1 # 快速建固定大小的文件

# 要压缩的目录或文件
source_dir="/mnt"
# 压缩文件的输出路径和名称
output_file="/tmp/mnt.tar.gz"
# 使用 tar 命令进行压缩，并通过 --exclude 参数过滤目录或文件
tar zcvf "$output_file" --exclude="/mnt/test" --exclude="/mnt/nginx.log" "$source_dir"


alias
    # vim ~/.bashrc ; source ~/.bashrc
    testFunc(){
    echo "hello, boge!"
    date
    }

    alias boge=testFunc


cat
	cat -A
	cat -n
    # cat > test.log << BOGE
    aaaaaaaaaa
    bbbbbbbb
    ccccccccc
    BOGE

```
