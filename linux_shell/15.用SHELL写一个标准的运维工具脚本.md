# https://www.toutiao.com/article/7284588575872860707/

运维在日常工作中，有很多机会要需要去写一个相应场景下的运维工具脚本，那么，我们该如何规范地写出这个脚本，这就是本节课程博哥要教大家的。

# 先列举下不是太规范的写法：

```
#!/bin/bash

ACTION="$1"
EXPIRY="$2"
USER_TYPE="$3"
USER_NAME="$4"

# 打印解析的参数
echo "ACTION: $ACTION"
echo "EXPIRY: $EXPIRY"
echo "USER_TYPE: $USER_TYPE"
echo "USER_NAME: $USER_NAME"

```

# 正规写法，使用 getopts 函数解析命令行参数

```
#!/bin/bash

# 这个脚本演示了如何使用 getopts 函数解析命令行参数
if [ $# -eq 0 ];then
    echo "Usage: bash $0 (-A|-D|-L) -e (1h or more) -t (admin|view) -u boge"
    exit 1
fi


# 默认值
ACTION=""
EXPIRY=""
USER_TYPE=""
USER_NAME=""

# 解析命令行参数
while getopts "ADLe:t:u:" OPTION; do
    case $OPTION in
        A)
            ACTION="add-test $1"
            ;;
            # ;; 的作用
            # 语句结束符: ;; 用于告诉 Bash，当前 case 分支的命令已经结束，不需要继续检查其他分支。
        D)
            ACTION="del-test $1"
            ;;
        L)
            ACTION="list-test $1"
            ;;
        e)

            EXPIRY="$OPTARG"   # $OPTARG固定写法， -e后的参数
            [[ $OPTARG =~ ^[1-9][0-9]*h$ ]] || { echo "'-e' must be set like '2h, 5h, 50000h, ...'"; exit 1; }
            ;;
        t)
            USER_TYPE="$OPTARG"
            [[ $OPTARG =~ ^(admin|view)$ ]] || { echo "'-t' can only be set as 'admin' or 'view'"; exit 1; }
            ;;
        u)
            USER_NAME="$OPTARG"
            ;;
        ?)
            echo "Invalid option: -$OPTARG"
            exit 1
            ;;
    esac
done

function add-test(){
# 打印解析的参数
echo "ACTION: $ACTION"
echo "EXPIRY: $EXPIRY"
echo "USER_TYPE: $USER_TYPE"
echo "USER_NAME: $USER_NAME"
}

function del-test(){
# 打印解析的参数
echo "ACTION: $ACTION"
echo "EXPIRY: $EXPIRY"
echo "USER_TYPE: $USER_TYPE"
echo "USER_NAME: $USER_NAME"
}

function list-test(){
# 打印解析的参数
echo "ACTION: $ACTION"
echo "EXPIRY: $EXPIRY"
echo "USER_TYPE: $USER_TYPE"
echo "USER_NAME: $USER_NAME"
}

${ACTION}


```

# 正则学习

[["aa" =~ a?]] || echo "can not valid"
[["aa" =~ ^a?$]] || echo "can not valid"

echo "aaaab" | grep -P "^a\*b$"    # * 0或多次 贪婪匹配，尽可能的匹配多个字符
echo "ab" | grep -P "^a+b$" # + 1 或多次 贪婪匹配，尽可能的匹配多个字符
echo "ab" | grep -P "^a?b$" # ? 0 或 1 次

echo "ab" | grep -P "^a?$" # 不会有输出
echo "ab" | grep -P "a?" # 会匹配成功
ab

# 基本概念

## 字符匹配

a: 匹配字符 a。
.: 匹配任何单个字符（除了换行符）。

## 字符集

[abc]: 匹配字符 a、b 或 c 中的任意一个。
[^abc]: 匹配除了 a、b、c 之外的任何字符。

## 范围

[a-z]: 匹配小写字母 a 到 z 之间的任何一个字符。
[0-9]: 匹配数字 0 到 9 之间的任何一个字符。

## 量词

```
*: 匹配前面的元素零次或多次（贪婪匹配）。例如 a* 匹配空字符串、a、aa 等。
+: 匹配前面的元素一次或多次（贪婪匹配）。例如 a+ 匹配 a、aa、aaa 等。
?: 匹配前面的元素零次或一次。例如 a? 匹配空字符串或 a。
{n}: 匹配前面的元素恰好 n 次。例如 a{3} 匹配 aaa。
{n,}: 匹配前面的元素至少 n 次。例如 a{2,} 匹配 aa、aaa、aaaa 等。
{n,m}: 匹配前面的元素至少 n 次，但不超过 m 次。例如 a{2,4} 匹配 aa、aaa、aaaa。

```

## 边界匹配

^: 匹配字符串的开头。例如 ^a 匹配以 a 开头的字符串。
$: 匹配字符串的结尾。例如 a$ 匹配以 a 结尾的字符串。
分组和捕获

()：用于分组。例如 (abc)+ 匹配 abc 重复出现一次或多次。
|：表示“或”。例如 a|b 匹配 a 或 b。

## 反向引用

\n：匹配第 n 个捕获组中的内容。例如 (a)\1 匹配 aa，因为 \1 参考第一个捕获组。

## 转义字符

\: 用于转义特殊字符。例如 \. 匹配字面上的 .。

## 预定义字符类

\d: 匹配数字字符，等同于 [0-9]。
\D: 匹配非数字字符，等同于 [^0-9]。
\w: 匹配字母、数字和下划线，等同于 [a-zA-Z0-9_]。
\W: 匹配非字母、数字和下划线，等同于 [^a-zA-Z0-9_]。
\s: 匹配空白字符（包括空格、制表符、换行符），等同于 [ \t\n\r\f\v]。
\S: 匹配非空白字符。
