# 介绍

与 ConfigMap 类似，用于存储配置信息，但是主要用于存储敏感信息、需要加密的信息，Secret 可以提供数据加密、解密功能。

在创建 Secret 时，要注意如果要加密的字符中，包含了有特殊字符，需要使用转义符转移，例如 $ 转移后为 \$，也可以对特殊字符使用单引号描述，这样就不需要转移例如 1$289*-! 转换为 '1$289*-!'

docker 私有镜像仓库的拉取

# 命令

kubectl create secret -h

kubectl create secret generic orig-secret --from-literal=username=admin --from-literal=password='@#$%'
特殊字符做 base64 转换的时候加单引号

echo "admin" | base64
echo "YWRtaW4K" | base64 --decode

kubectl create secret docker-registry -h

kubectl create secret docker-registry harbor-secret --docker-username=admin --docker-password=harbor12345 --docker-email=admin@wolfcode.com
kubectl edit secret/harbor-secret

指定服务器地址
kubectl create secret docker-registry harbor-secret --docker-username=admin --docker-password=harbor12345 --docker-email=admin@wolfcode.com --docker-server=192.168.1.12:8858

## 解密

echo 'eyJhdXRocyI6eyJodHRwczovL2luZGV4LmRvY2tlci5pby92MS8iOnsidXNlcm5hbWUiOiJhZG1pbiIsInBhc3N3b3JkIjoid29sZmNvZGUiLCJlbWFpbCI6ImxpdWdhbmdAd29sZmNvZGUuY24iLCJhdXRoIjoiWVdSdGFXNDZkMjlzWm1OdlpHVT0ifX19' | base64 --decode
这样 secret 的加密信息都会得到解密出来

# 测试 secret

进到 node2 中

## 列出来都有哪些镜像

docker images

docker tag nginx:1.9.1 192.168.1.12:8858/opensource/nginx:1.9.1

## 登录 到 harbor

docker login -uadmin 192.168.1.12:8858
密码: harbor12345

## 上传操作

docker push 192.168.1.12:8858/opensource/nginx:1.9.1

## 准备 private-image-pull-pod.yaml

```
apiVersion: v1
kind: Pod
metadata:
  name: private-image-pull-pod
spec:
  imagePullSecrets:   #配置登录docker registry的secret
  - name: harbor-secret
  containers:
    - name: nginx
      image: 192.168.1.12:8858/opensource/nginx:1.9.1
      command: ["/bin/sh", "-c", "sleep 3600"]
      imagePullPolicy: IfNotPresent
      env:
        - name: JAVA_VM_OPTS
          valueFrom:
            configMapKeyRef:
              name: test-env-config  #config map 的名字
              key: JAVA_OPTS_TEST # 从name的 configmap中获取名字为key的value，将其赋值给本地环境变量 JAVA_VM_OPTS
        - name: APP
          valueFrom:
            configMapKeyRef:
              name: test-env-config  #config map 的名字
              key: APP_NAME
      volumeMounts: #加载数据卷
        - name: db-config  #表示加载volumns属性中哪个数据卷
          mountPath: "/usr/local/mysql/confi" #想要将数据卷中的文件加载到哪个目录下
          readOnly: true #是否只读
  volumes:  #数据卷挂载 configmap， secret
    - name: db-config   #数据卷的名字，可以随意
      configMap:  #数据卷的类型为 configMap
        name: tes-dir-config  # configMap 的名字，需要跟加载的configMap名字相同
        items: #对configMap中的key进行映射，如果不指定，默认会将configMap中所有key全部转换为一个个同名的文件
          - key: "db.properties"  #configMap 中的key
            path: "db.properties1"  #将该key的值转换为文件
  restartPolicy: Never

```

删掉 Node2 中已经存在的镜像，否则不会走下载

docker rmi 192.168.1.12:8858/opensource/nginx:1.9.1
docker logout # 登出
docker logout 192.168.1.12:8858 # 登出
docker pull 192.168.1.12:8858/opensource/nginx:1.9.1 # 应该失败
