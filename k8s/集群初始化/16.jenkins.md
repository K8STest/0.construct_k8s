# 构建带 maven 环境的 jenkins 镜像

docker build -t 192.168.1.12:8858/library/jenkins-maven:v1 .

--docker build -t 192.168.1.12:8858/library/jenkins-maven:jdk-11 .

# 登录 harbor

docker login -uadmin 192.168.1.12:8858

# 推送镜像到 harbor

-- docker push 192.168.1.12:8858/library/jenkins-maven:jdk-11
docker push 192.168.1.12:8858/library/jenkins-maven:v1

# 安装 Jenkins

```
# 进入 jenkins 目录，安装 jenkins

kubectl apply -f manifests/

# 查看是否运行成功

kubectl get po -n kube-devops

# 查看 service 端口，通过浏览器访问

kubectl get svc -n kube-devops

# 查看容器日志，获取默认密码

kubectl logs -f pod 名称 -n kube-devops

```

# vim 设定行号

:set nu

# 安装 jenkins 后

kubectl logs -f jenkins-5c5d5cdbdb-x7rzx -n kube-devops
密码
e616d9bf42ba461cb32e970f3007e7a2

admin
新密码
--ACSdev312!@

---

---

---

Jenkins initial setup is required. An admin user has been created and a password generated.
Please use the following password to proceed to installation:

e616d9bf42ba461cb32e970f3007e7a2

This may also be found at: /var/jenkins_home/secrets/initialAdminPassword

---

---

---

如果有些插件搜不到，可以去官网下载 https://plugins.jenkins.io/

# 需要配置的 SonarQube Scanner

代码质量审查工具

在 Dashboard > 系统管理 > Configure System 下面配置 SonarQube servers

Name：sonarqube # 注意这个名字要在 Jenkinsfile 中用到
Server URL：http://sonarqube:9000
Server authentication token：创建 credentials 配置为从 sonarqube 中得到的 token

进入系统管理 > 全局工具配置 > SonarQube Scanner > Add SonarQube Scanner
Name：sonarqube-scanner
自动安装：取消勾选
SONAR_RUNNER_HOME：/usr/local/sonar-scanner-cli

# 如何访问服务

服务名.命名空间

# Kubernetes

jenkins + k8s 环境配置

进入 Dashboard > 系统管理 > 节点管理 > Configure Clouds 页面

配置 k8s 集群
名称：kubernetes
点击 Kubernetes Cloud details 继续配置
Kubernetes 地址：
如果 jenkins 是运行在 k8s 容器中，直接配置服务名即可
https://kubernetes.default
如果 jenkins 部署在外部，那么则不仅要配置外部访问 ip 以及 apiserver 的端口（6443），还需要配置服务证书
Jenkins 地址：
如果部署在 k8s 集群内部：http://jenkins-service.kube-devops
如果在外部：http://192.168.113.120:32479（换成你们自己的）

配置完成后保存即可

# 创建 gitlab 访问凭证

# 替代吗到 gitlab 中

git clone http://192.168.1.11:28080/gitlab-instance-e3d33d96/k8s-cicd-demo.git

# 配置 git

git config --global user.email "admin@example.com"
git config --global user.name "Administrator"
