# harbor 的下载包 node2 节点

wget https://github.com/goharbor/harbor/releases/download/v2.5.0/harbor-offline-installer-v2.5.0.tgz

tar -zxvf harbor-offline-installer-v2.5.0.tgz

# 启动 harbor

systemctl daemon-reload
systemctl restart docker
cd /opt/k8s/harbor/harbor && /usr/local/bin/docker-compose up -d

# 重启 harbor

cd /opt/k8s/harbor/harbor # 替换为你的 Harbor 安装目录
sudo docker-compose down
sudo docker-compose up -d

# 向 daemon.json 中添加内容， 进入到 node1 节点

vim /etc/docker/daemon.json

systemctl daemon-reload
systemctl restart docker

# 查看情况

docker info

docker login -uadmin 192.168.1.12:8858

systemctl daemon-reload
systemctl restart docker
