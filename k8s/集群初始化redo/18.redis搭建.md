cd /opt/k8s/devops/microservices/

# 先安装 redis

cd /opt/k8s/devops/microservices/basics/
helm install redis ./redis/ -n redis
kubectl get -n redis
kubectl get svc -n redis

密码
wolfcode123

链接上 redis 客户端，数据库手动添加一个 key
在 redis

[root@k8s-master basics]# kubectl exec -it redis-replicas-0 -n redis -- bash
I have no name!@redis-replicas-0:/$ redis-cli
127.0.0.1:6379> auth wolfcode123
OK
127.0.0.1:6379> get test
"wolfcode"

redis-master.redis:6379
