# 关闭防火墙

systemctl stop firewalld
systemctl disable firewalld

# 关闭 selinux

sed -i 's/enforcing/disabled/' /etc/selinux/config # 永久
setenforce 0 # 临时

# 关闭 swap

swapoff -a # 临时
sed -ri 's/._swap._/#&/' /etc/fstab # 永久

# 关闭完 swap 后，一定要重启一下虚拟机！！！

# 根据规划设置主机名

hostnamectl set-hostname k8s-master

# 在 master 添加 hosts

cat >> /etc/hosts << EOF
192.168.1.10 k8s-master
192.168.1.11 k8s-node1
192.168.1.12 k8s-node2

EOF

# 将桥接的 IPv4 流量传递到 iptables 的链

cat > /etc/sysctl.d/k8s.conf << EOF
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
EOF

sysctl --system # 生效

# 时间同步

yum install ntpdate -y
ntpdate time.windows.com

# 再次初始化集群时候需要有的是

# 然后安装 各个节点里安装 docker

```
#ingress controller
#sc
#harbor
#gitlab
#nfs 服务器的 磁盘要稍微大一些
```

# kubesphere

http://192.168.1.12:30880/clusters/default/components

admin
密码
P@88w0rd

# harbor

http://192.168.1.12:8858/harbor/projects
admin
Harbor12345

# gitlab

http://192.168.1.11:28080

用户信息
root

/etc/gitlab/initial_root_password

--wnkE2RX4JH0bFbeQDzD/iR4E8PRoWm1gIqXfl7OYrHA=
新密码：ACSdev312!@

# sonarqube

http://192.168.1.10:32163/
admin
ACSdev312!@

```
systemctl daemon-reload
systemctl restart docker

cd /opt/k8s/harbor/harbor # 替换为你的 Harbor 安装目录
sudo docker-compose down
sudo docker-compose up -d


```

# jenkins

http://192.168.1.10:30653/login?from=%2F
密码
e616d9bf42ba461cb32e970f3007e7a2

admin
新密码
ACSdev312!@
